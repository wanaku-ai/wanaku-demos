mcp:
  tools:
    - cat-facts-route:
        route:
          id: "cat-facts-route"
        description: "Retrieve random facts about cats"
        properties:
          - name: count
            type: int
            description: Number of cat facts to retrieve
            required: true
            mapping:
              type: header
              name: COUNT
